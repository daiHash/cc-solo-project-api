datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Remove this model when actually starting developing this is just for test
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}